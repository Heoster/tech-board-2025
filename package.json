{
  "name": "mcq-testing-system",
  "version": "1.0.0",
  "description": "MCQ Testing System for TECH BOARD 2025 Selection Test",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "client:dev": "cd client && npm run dev",
    "server:dev": "cd server && npm run dev",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && npm start",
    "railway:start": "cd server && node railway-complete-start.js",
    "railway:build": "echo 'Building client...' && cd client && npm install && npm run build && echo 'Client build complete' && cd ../server && npm install && echo 'Server dependencies installed'",
    "postinstall": "cd server && npm install"
  },
  "devDependencies": {
    "axios": "^1.11.0",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "dotenv": "^17.2.1"
  }
}
